---
description: Preview the DevPilot Studio status bar, stats panel, and history chart with sample data
allowed-tools: Bash(bash:*), Bash(dps:*), Bash(devpilot:*)
---

Show the user a **DevPilot Studio** live preview using sample data.

Run all three demo modes so the user can see what the status bar looks like:

**Status Bar Preview:**

!`"${CLAUDE_PLUGIN_ROOT}/bin/devpilot" demo`

**Expanded Stats Panel Preview:**

!`"${CLAUDE_PLUGIN_ROOT}/bin/devpilot" demo stats`

**7-Day History Chart Preview:**

!`"${CLAUDE_PLUGIN_ROOT}/bin/devpilot" demo history`

Tell the user they can customize the appearance with `/devpilot-studio:configure`.
